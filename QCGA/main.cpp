#include "QCGA.h"
#include "Blade.h"

#include <iostream>




int main()
{
	//TEST FRO REPO
	QCGA::generateGeneratingBlades();
	QCGA obj = e1 * e2 * e3;
	Blade a = e1 + e2;
	std::cout << a << std::endl;
	std::cout << (a^(-1)) << std::endl;
	std::cout << (a* (a ^ (-1))) << std::endl; 
	return 0;
}
